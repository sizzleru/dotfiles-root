[background]
path = "/etc/greetd/regreet/background.jpg"
fit = "Cover"

[GTK]
style_path = "/etc/greetd/regreet/style.css"

[commands]
reboot = [ "systemctl", "reboot" ]
poweroff = [ "systemctl", "poweroff" ]
